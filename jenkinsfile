pipeline {
    agent any

    triggers {
        githubPush()
    }

    stages {
        stage('Webhook Triggered') {
            steps {
                echo "✅ Webhook is working! Build triggered by GitHub push event."
            }
        }
    }
}
